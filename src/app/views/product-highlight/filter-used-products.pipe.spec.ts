import { FilterUsedProductsPipe } from './filter-used-products.pipe';

describe('FilterUsedProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUsedProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
